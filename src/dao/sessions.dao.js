// DAO de sesiones.
// Estructura inicial: en próximas entregas contendrá la lógica de
// autenticación (login, JWT, cookies, Passport).

class SessionsDao {
    async getCurrent() {
        return null;
    }
}

export default new SessionsDao();
